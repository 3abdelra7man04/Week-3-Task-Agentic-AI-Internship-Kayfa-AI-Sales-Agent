{
  "name": "uniask-dashboard",
  "version": "1.0.0",
  "description": "UniAsk RAG Admin Dashboard — plain HTML/CSS/JS, no build step required",
  "scripts": {
    "start": "npx live-server --port=3000 --open=index.html",
    "dev": "npx live-server --port=3000 --open=index.html"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "live-server": "^1.2.2",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.2.4"
  },
  "notes": {
    "no-bundler": "This project uses React via CDN + Babel standalone. No webpack/vite needed.",
    "vs-code": "Install the 'Live Server' extension by Ritwick Dey, then right-click index.html → 'Open with Live Server'."
  }
}
